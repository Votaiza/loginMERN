{
  "name": "login",
  "version": "1.0.0",
  "description": "Login utilizando el stack MERN",
  "main": "main.js",
  "scripts": {
    "dev": "set NODE_ENV=development && nodemon main.js --exec",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "set NODE_ENV=production && node main.js --exec"
  },
  "author": "Victor Otaiza",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "dotenv": "^16.0.1",
    "express": "4.18.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
